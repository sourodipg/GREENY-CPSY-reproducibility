# BranchN freeze rule

BranchN is additive only.

No file outside `branchN/` is changed by the BranchN experiment scripts.
The frozen production executable is consumed as a black-box implementation authority.
Temporary per-run directories receive only the executable's generated result files.
The manuscript is not edited by BranchN.
